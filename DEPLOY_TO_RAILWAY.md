# 🚀 Deploy TeachMetrics ขึ้น Railway

## ✅ สิ่งที่เตรียมไว้แล้ว

โปรเจกต์นี้พร้อม deploy บน Railway แล้ว! ไฟล์ที่สำคัญ:

- ✅ **Dockerfile** - สำหรับ build Docker image
- ✅ **Procfile** - คำสั่งเริ่มต้นแอปพลิเคชัน
- ✅ **.railwayignore** - ไฟล์ที่ไม่ต้อง deploy
- ✅ **GitHub Repository** - โค้ดอยู่ที่ https://github.com/onuma5290-code/TeachMetrics

---

## 📋 ขั้นตอนการ Deploy (แนะนำ)

### ขั้นตอนที่ 1: เปิด Railway Dashboard

1. ไปที่: **https://railway.app**
2. Login ด้วย GitHub account
3. คลิก **"New Project"**

### ขั้นตอนที่ 2: Deploy from GitHub

1. เลือก **"Deploy from GitHub repo"**
2. เลือก repository: **onuma5290-code/TeachMetrics**
3. เลือก branch: **main**
4. คลิก **"Deploy Now"**

Railway จะเริ่ม build และ deploy อัตโนมัติ!

### ขั้นตอนที่ 3: ตั้งค่า Environment Variables

**⚠️ สำคัญมาก!** ต้องตั้งค่าตัวแปรเหล่านี้ไม่งั้นจะเกิด 500 Error

1. คลิกที่ service ที่เพิ่งสร้าง
2. ไปที่แท็บ **"Variables"** (ด้านบน)
3. คลิก **"New Variable"** และเพิ่มตัวแปรเหล่านี้:

```env
APP_KEY=base64:P6MgqjWHjCWsSiGuCR8YKBggOHJLUwGZ857sXZ3zGE6U=
APP_NAME=TeachMetrics
APP_ENV=production
APP_DEBUG=false
APP_URL=https://your-app-name.up.railway.app
DB_CONNECTION=sqlite
LOG_LEVEL=error
SESSION_DRIVER=file
CACHE_STORE=file
QUEUE_CONNECTION=sync
```

**หมายเหตุ:** อัพเดต `APP_URL` ให้ตรงกับ domain ที่ Railway สร้างให้

4. หลังจากเพิ่มครบแล้ว คลิก **"Redeploy"** หรือรอให้ Railway redeploy อัตโนมัติ

### ขั้นตอนที่ 4: สร้าง Public Domain

1. ในหน้า service ไปที่แท็บ **"Settings"**
2. เลื่อนลงหา **"Networking"** หรือ **"Domains"**
3. คลิก **"Generate Domain"**
4. คัดลอก URL ที่ได้ (เช่น `https://teachmetrics-production.up.railway.app`)
5. กลับไปที่ **Variables** และอัพเดต `APP_URL` ให้ตรงกับ domain นี้

### ขั้นตอนที่ 5: ตรวจสอบการ Deploy

1. ไปที่แท็บ **"Deployments"**
2. คลิกที่ deployment ล่าสุด
3. ดู **Build Logs** และ **Deploy Logs**
4. รอจนกว่าสถานะจะเป็น **"Success"** สีเขียว

### ขั้นตอนที่ 6: เปิดเว็บไซต์

1. คลิกที่ URL ที่สร้างไว้
2. หรือคลิกปุ่ม **"View Deployment"**
3. เว็บไซต์ควรทำงานได้แล้ว! 🎉

---

## 🔄 การอัพเดตโค้ด

เมื่อต้องการอัพเดตโค้ด:

1. แก้ไขโค้ดในเครื่อง
2. Commit และ Push ขึ้น GitHub:
   ```bash
   git add .
   git commit -m "Your update message"
   git push origin main
   ```
3. Railway จะ detect การเปลี่ยนแปลงและ deploy ใหม่อัตโนมัติ

---

## 🗄️ ใช้ PostgreSQL แทน SQLite (ถ้าต้องการ)

### เพิ่ม PostgreSQL Database:

1. ใน Railway Project คลิก **"+ New"**
2. เลือก **"Database"** → **"PostgreSQL"**
3. Railway จะสร้าง environment variables อัตโนมัติ

### อัพเดต Environment Variables:

ลบหรือแก้ไขตัวแปรเหล่านี้:

```env
DB_CONNECTION=pgsql
DB_HOST=${PGHOST}
DB_PORT=${PGPORT}
DB_DATABASE=${PGDATABASE}
DB_USERNAME=${PGUSER}
DB_PASSWORD=${PGPASSWORD}
```

Railway จะแทนที่ `${PGHOST}`, `${PGPORT}` ฯลฯ อัตโนมัติ

---

## ❗ Troubleshooting

### 500 Internal Server Error

**สาเหตุ:** ไม่มี `APP_KEY` หรือ environment variables ไม่ครบ

**วิธีแก้:**
1. ตรวจสอบว่าตั้งค่า `APP_KEY` แล้ว
2. ตรวจสอบว่ามี environment variables ครบตามที่ระบุ
3. Redeploy service

### Build Failed

**สาเหตุ:** Dockerfile หรือ dependencies มีปัญหา

**วิธีแก้:**
1. ดู Build Logs ใน Railway Dashboard
2. ตรวจสอบ error message
3. แก้ไขปัญหาและ push ขึ้น GitHub ใหม่

### Database Error

**สาเหตุ:** SQLite file ไม่ได้สร้างหรือ migrations ไม่รัน

**วิธีแก้:**
1. ตรวจสอบ Deploy Logs ว่า migrations รันสำเร็จหรือไม่
2. ตรวจสอบว่า `DB_CONNECTION=sqlite` ถูกตั้งค่าแล้ว
3. Dockerfile มีคำสั่ง `php artisan migrate --force` อยู่แล้ว

### Application Not Responding

**สาเหตุ:** Port ไม่ถูกต้องหรือ service ไม่ start

**วิธีแก้:**
1. ตรวจสอบ Deploy Logs
2. ตรวจสอบว่า Dockerfile ใช้ `${PORT}` variable
3. ตรวจสอบว่า service กำลัง running อยู่

---

## 📚 ข้อมูลเพิ่มเติม

### สิ่งที่ Railway ทำอัตโนมัติ:

1. ✅ Build Docker image จาก Dockerfile
2. ✅ ติดตั้ง PHP 8.4 และ dependencies
3. ✅ รัน `composer install`
4. ✅ สร้าง SQLite database
5. ✅ รัน migrations
6. ✅ Cache config, routes, views
7. ✅ เริ่ม Laravel server

### ไฟล์สำคัญ:

- **Dockerfile** - กำหนดวิธี build application
- **Procfile** - กำหนดคำสั่ง start (ถ้าไม่ใช้ Dockerfile)
- **.railwayignore** - ไฟล์ที่ไม่ต้อง deploy

### ลิงก์ที่เป็นประโยชน์:

- [Railway Documentation](https://docs.railway.app)
- [Laravel Deployment Guide](https://laravel.com/docs/deployment)
- [Railway Community](https://help.railway.app)

---

## 🎯 สรุป

1. **Deploy from GitHub** - เชื่อมต่อ repository
2. **ตั้งค่า Environment Variables** - เพิ่มตัวแปรที่จำเป็น
3. **Generate Domain** - สร้าง public URL
4. **เปิดเว็บไซต์** - ทดสอบว่าทำงานได้

**พร้อม Deploy แล้ว! 🚀**

---

## 📞 ต้องการความช่วยเหลือ?

ถ้ามีปัญหาหรือคำถาม:
1. ตรวจสอบ Logs ใน Railway Dashboard
2. ดู Troubleshooting section ด้านบน
3. ติดต่อ Railway Support: https://help.railway.app
